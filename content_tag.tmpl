{{define "content"}}

<h2>{{ .Title }}</h2>
<ul class="post-list">
    {{ range .Posts }}
    <li>
        <span class="post-date">{{ .Date.Format "Jan 2, 2006" }}</span>
        <a href="/{{ .Slug }}" class="post-title">{{ .Title }}</a>
    </li>
    {{ end }}
{{ end }}