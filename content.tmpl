{{define "content"}}
    <article>
        <h1>{{ .FrontMatter.Title }}</h1>
        <span class="pub-date">Published on {{ .FrontMatter.Date }}</span>
        <div>{{ .Content }}</div>
        
    </article>
{{end}}